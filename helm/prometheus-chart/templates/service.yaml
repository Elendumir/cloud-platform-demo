apiVersion: v1
kind: Service
metadata:
  name: {{ include "prometheus-chart.fullname" . }}
  labels:
    {{- include "prometheus-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prometheus-chart.selectorLabels" . | nindent 4 }}
